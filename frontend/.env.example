# Copy to .env.local if you need overrides.
# VITE_API_KEY: sent as x-api-key. In `npm run dev`, client defaults to local-dev if unset.
# For production (`npm run build`), set VITE_API_KEY to the real key.

VITE_API_BASE=http://127.0.0.1:8000
VITE_API_KEY=
